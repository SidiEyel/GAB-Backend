### Spring Boot CRUD Application

## Overview

This project is a Spring Boot application designed to perform CRUD (Create, Read, Update, Delete) operations on grades. The application is built with adherence to SOLID principles and includes functionality to transfer grades.
